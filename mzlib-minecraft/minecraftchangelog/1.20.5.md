- 将物品 nbt tag 改为了各种组件
  - 组件是可序列化的常量，修改时产生新的组件对象
  - 物品组件表包含默认表和差异 map，序列化时只保存差异表
  - 物品拷贝时，仅浅克隆组件表，并将两个组件表标记为 copyOnWrite
  - 更改组件表时，若存在 copyOnWrite 标记，则浅克隆差异 map，并删除标记
- GameProfile 使用组件包了一层，组件中的 id 和 name 可空